# CryoBrain 2-minute demo script (SPEC-v6.1 C10)

Measured artifacts only. Open `demo/index.html` offline after `scripts/run_demo_build_wsl.sh`.

1. **Concept (10s):** Every quantum chip needs a brain — CryoBrain is the swarm that designs that brain.
2. **Swarm bus (30s):** Show event log strip — Research → Architect → RTL → Measurement → Verifier → Scorer → Memory. Point at `prompt_influenced: true` and live Exa URLs when online.
3. **Climb proof (20s):** `artifacts/measured_climb.json` — Architect measured suppression on `score_measured`.
4. **Pareto (20s):** `artifacts/measured_pareto.json` — 8 measured points, LER vs latency/area tradeoffs.
5. **FIFO platform (20s):** `artifacts/measured_fifo_climb.json` — throughput 0.09 → 0.44 measured.
6. **Close (20s):** Slow loop is built tonight; fast in-fridge loop is the 2040 arc.

**Backup:** Screen recording of the above using committed artifacts; label concept visuals.